[package]
name = "cuda-discovery"
version = "0.1.0"
edition = "2021"
description = "Service discovery — agent registration, health probing, capability advertisement, and fleet directory for locating agents"

[dependencies]
serde = { version = "1", features = ["derive"] }
