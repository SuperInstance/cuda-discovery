[package]
name = "cuda-discovery"
version = "0.1.0"
edition = "2021"
description = "Agent discovery — capability broadcasting, peer finding, service registry, and fleet topology awareness"

[dependencies]
serde = { version = "1", features = ["derive"] }
